Requer **poetry 2.0+**.

Clone o repositório e instale as dependências de desenvolvimento:

```bash
poetry install --with dev,doc # testes, lints e documentação (Recomendado)
poetry install --with dev     # só testes e lint
```
